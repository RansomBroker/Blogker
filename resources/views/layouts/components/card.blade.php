<div class="card {{ $cardOption }}"  >

  <div class="card-header {{ $cardHeadOption }}" >
    {{ $cardHeadTitle }}
  </div>

  <div class="card-body {{ $cardBodyOption }}" >
    {{ $slot }}
  </div>

</div>
